import React from 'react';
import LearnReact from './lib/LearnReact';

function App({ config }) {
  return (
    <div className='App'>
      <LearnReact config={config} />
    </div>
  );
}

export default App;
